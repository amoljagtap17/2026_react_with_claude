Look up MUI Material UI v7 documentation to answer the following question or complete the task:

$ARGUMENTS

Steps:

1. Call `mcp__mui-mcp__useMuiDocs` with `urlList: ["https://llms.mui.com/material-ui/7.2.0/llms.txt"]` to get the index of available docs pages.
2. From the returned index, identify the most relevant documentation URLs for the question/task.
3. Call `mcp__mui-mcp__fetchDocs` with those URLs to retrieve the actual documentation content.
4. Use the fetched docs to answer the question or implement the requested changes, following MUI v7 best practices.
