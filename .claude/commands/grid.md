Create a grid following the project's grid skill.

$ARGUMENTS

Follow all instructions in `.claude/skills/grid/SKILL.md` using the arguments above.
Reference `.claude/skills/grid/reference.md` for the AgGrid component API and column recipes.
Reference `.claude/skills/grid/examples.md` for the canonical AdminGrid example.
